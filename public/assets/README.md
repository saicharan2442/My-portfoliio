# Assets

Drop your manually-added media here:

- `intro-video.mp4` — fullscreen cinematic intro video (auto-plays on load)
- `profile-image.png` — profile photo for the About section

These files are referenced from `/assets/...` at runtime. The site gracefully
falls back to placeholder visuals if a file is missing, so you can add them
anytime without rebuilding the layout.
